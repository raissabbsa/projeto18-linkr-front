import { createConfig, login, register, searchUsers, follow, unfollow, getFollowers } from "./users";

const api = {
	createConfig,
	login,
	register,
	searchUsers,
	follow,
	unfollow,
	getFollowers
};

export default api;
